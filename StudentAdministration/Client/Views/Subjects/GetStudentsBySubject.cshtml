@* @model IEnumerable<StudentAdministration.Client.Models.Subjects.GetStudentsBySubjectViewModelItem>

@{
    ViewData["Title"] = "Students";
}

<partial name="_Notification" />

<h1 style="text-align:center">Students</h1>
 *@
@* <table class="table" style="text-align:center">
    <thead>
        <tr>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentIndex)
            </th>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentFullName)
            </th>
            <th style="width:15%">
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th style="width:15%"></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <form asp-action="SetGrade" asp-controller="Subjects">
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StudentIndex)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StudentFullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Grade)
                </td>
                <td>
                    @Html.ActionLink("Give grade", "SetGrade", "Subjects", new
                        {
                            subjectId = item.SubjectId,
                            subjectPartitionKey = item.SubjectPartitionKey,
                            studentId = item.StudentId,
                            studentPartitionKey = item.StudentPartitionKey,
                            grade = item.Grade
                        })
                </td>
            </tr>
        </form>
    }
    </tbody>
</table> *@

@* <div asp-validation-summary="ModelOnly" class="text-danger"></div>
<table class="table table-hover table-responsive-sm display">
    <thead>
        <tr>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentIndex)
            </th>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentFullName)
            </th>
            <th style="width:15%">
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th style="width:15%"></th>
        </tr>
    </thead>
</table>
@foreach (var item in Model)
{
    <form asp-action="Index">
        <table class="table table-hover table-responsive-sm display">
            <tbody>
                <tr>
                    <td>
                        @Html.HiddenFor(modelItem => item.SubjectId)
                    </td>
                    <td>
                        @Html.HiddenFor(modelItem => item.SubjectPartitionKey)
                    </td>
                    <td>
                        @Html.HiddenFor(modelItem => item.StudentId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudentIndex)
                    </td>
                    <td>
                        @Html.HiddenFor(modelItem => item.StudentPartitionKey)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StudentFullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Grade)
                    </td>
                    <td>
                        <div class="form-group">
                            <button type="submit" name="action">Set grade</button>
                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
    </form>
} *@

@model IEnumerable<StudentAdministration.Client.Models.Subjects.GetStudentsBySubjectViewModelItem>

@{
    ViewData["Title"] = "Students";
}

<partial name="_Notification" />

<h1 style="text-align:center">Students</h1>

<table class="table table-hover table-responsive-sm display" style="text-align:center">
    <thead>
        <tr>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentIndex)
            </th>
            <th style="width:30%">
                @Html.DisplayNameFor(model => model.StudentFullName)
            </th>
            <th style="width:15%">
                @Html.DisplayNameFor(model => model.Grade)
            </th>
            <th style="width:15%"></th>
        </tr>
    </thead>
</table>

@foreach (var item in Model)
{
    <form asp-controller="Subjects" asp-action="SetGrade" method="post">
        <input type="hidden" name="SubjectId" value="@item.SubjectId" />
        <input type="hidden" name="SubjectPartitionKey" value="@item.SubjectPartitionKey" />
        <input type="hidden" name="StudentId" value="@item.StudentId" />
        <input type="hidden" name="StudentPartitionKey" value="@item.StudentPartitionKey" />

        <table class="table table-hover table-responsive-sm display" style="text-align:center">
            <tbody>
                <tr>
                    <td style="width:30%">@Html.DisplayFor(modelItem => item.StudentIndex)</td>
                    <td style="width:30%">@Html.DisplayFor(modelItem => item.StudentFullName)</td>
                    <td style="width:15%">
                        <input type="number" name="Grade" value="@item.Grade" />
                    </td>
                    <td style="width:15%">
                        <button type="submit" class="btn btn-primary form-group">Set Grade</button>
                    </td>
                </tr>
            </tbody>
        </table>
    </form>
}
